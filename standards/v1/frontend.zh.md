# 前端应用规范（v1）

tags: frontend, 前端

## 1. 组件与状态

- 组件单一职责；展示与逻辑分离；样式与逻辑分离（css-modules 或独立样式文件）。
- 状态提升最小化；跨组件通信优先 props/组合，避免全局单例泛滥。

## 2. 数据通道

- 所有后端调用集中在一个 api 层文件；组件不直接 fetch。
- 错误必须呈现给用户（toast/内联提示），禁止静默吞错。

## 3. 交付

- 完成后本地构建通过（npm run build / tsc --noEmit）再交付；
- 关键交互流程写中文注释。
