/**
 * 主脑纪律注入 —— 主脑路由开启时向全部会话注入的分工约定（0.26.2）。
 *
 * 背景：主脑路由只保证「派出去的子代理走工人模型」，但管不住主模型亲自
 * 下场干活（实测 GPT 主脑 44 次工具调用里 39 次是亲自 read/edit，Token
 * 大量浪费在它本该委派的实现工作上）。本节把分工写死进系统提示：
 * 主模型只做规划、方法论、指挥与验收，一切实现类操作必须委派 subagent。
 *
 * 文案即纪律边界：既约束「不做什么」（禁止亲自读写/检索/执行），也给出
 * 「怎么做」（自包含委派、并行派活、失败重派不接手），并把指挥类轻量工具
 * 列为例外，避免模型因无所适从而回退到亲自干。
 */

/** 节名（宿主全局唯一；与沙箱纪律、项目约束节并列）。 */
export const BRAIN_ROUTER_DISCIPLINE_SECTION_NAME = 'plugin:dsh-devforge:brain-router-discipline'

/** 节顺序：项目约束注入(80)之后、沙箱纪律(90)之前，同属约束类。 */
export const BRAIN_ROUTER_DISCIPLINE_SECTION_ORDER = 85

/** 主脑路由启用时注入的分工纪律文本（text 求值返回，未启用返回空串）。 */
export const BRAIN_ROUTER_DISCIPLINE_TEXT = [
  '【主脑纪律（dsh-devforge 注入，必须遵守）】',
  '主脑路由已启用：你是大脑/项目管理员，工人模型负责一切实现。目标是把主脑的高价 Token 只花在思考与决策上。',
  '1. 你只做五件事：理解需求、拆解计划、给出方法论、派活（subagent 工具）、验收结果。禁止亲自读源码、改文件、grep/glob 搜索、跑 bash/pwsh 命令、操作浏览器。',
  '2. 委派写法：subagent 的 prompt 必须自包含（工人看不到本会话），写清目标、涉及的绝对路径、约束与验收标准；多个独立任务在同一条消息里并行派出。',
  '3. 需要信息就让工人查了汇报，需要改代码就派工人改；验收只依据子代理返回的结果，必要时再派一个复核子代理，不亲自重读文件。',
  '4. 子代理失败时优先改进任务描述后重派，不要接手自己干；只有委派无法完成的纯对话与决策才亲自回答。',
  '5. 回复保持精炼：计划用条目，不复述代码，不贴长日志；思考也按需克制。',
  '6. 例外（指挥职责，可直接用）：todo_write、list_agents、send_message、ask_user_question、goal 工具，以及不涉及任何文件/命令的纯回答。',
].join('\n')
